# Kamal config para katana.igorv.org — convive en el mismo VPS que
# bloqs.cl, memoriaqr.cl, lozarate y agentesgpt (kamal-proxy enruta
# por dominio). Un solo contenedor Node sirve el juego y el WebSocket
# de emparejamiento (~40 MB de RAM).
# OJO: igorv.org (raíz) tiene sitio y correo en Hostinger — solo se
# usa el subdominio, que no toca nada de eso.

service: katana
image: arigatodon/katana-fight

servers:
  web:
    - 72.60.156.215

proxy:
  ssl: true
  host: katana.igorv.org
  app_port: 8081
  healthcheck:
    path: /up
    interval: 5

registry:
  username: arigatodon
  password:
    - KAMAL_REGISTRY_PASSWORD

builder:
  arch: amd64

aliases:
  logs: app logs -f
  shell: app exec --interactive --reuse "sh"
